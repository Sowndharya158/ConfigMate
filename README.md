# ConfigMate

ConfigMate is a React + Vite web application designed to help users upload, validate, and manage firewall rules (e.g., Palo Alto Networks) via an Excel spreadsheet. It converts uploaded data into JSON, performs validations, and sends it to the backend for processing.
